// Availability checks are handled by
// /api/customer/bookings/availability. Keep this legacy route file as a
// module so Next.js can type-check the route tree without exposing a second
// unauthenticated handler.
export {};
