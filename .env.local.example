# MongoDB Atlas
MONGODB_URI="mongodb+srv://<user>:<password>@cluster.mongodb.net/cleannest?retryWrites=true&w=majority"

# Auth
AUTH_SECRET="replace-with-a-random-secret"

# Email (OTP delivery)
EMAIL_SERVER=""
EMAIL_FROM=""
